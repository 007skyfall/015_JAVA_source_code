1、声明一个Status状态的枚举类，
（1）它有四个常量对象Busy忙，Free闲，Vocation休假，Left离职
（2）声明一个属性：String desc;
例如：忙的描述信息为“忙得热火朝天，无法再安排其他任务”
（3）声明一个无参构造和有参构造
部分对象用无参构造，部分对象用有参构造
（4）重写toString()，显示对象的desc信息

2、声明一个Employee员工类，
有属性：姓名、薪资、状态
重写toString，显示员工的信息和状态
3、在测试类中创建Employee数组，长度为5
分别赋值为不同的员工对象，不同的员工对象状态不同

4、遍历数组，并显示员工信息
