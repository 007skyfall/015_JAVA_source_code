自动类型转换：
long -->float
8个字节 4个字节
为什么说4个字节的float的存储范围比8个字节的long类型的大
小数在底层如何存储？也是用二进制存储
小数在底层：符号位 + 指数部分 + 尾数部分  和整数的存储不一样，
所以long虽然是8个字节，但是它的存储范围没有float的类型大。

double类型的尾数是52位，float是23位，所以double的精度比float大

因为小数部分转二进制可能是无限的，例如2.2，所以float和double是不精确，是浮点数
